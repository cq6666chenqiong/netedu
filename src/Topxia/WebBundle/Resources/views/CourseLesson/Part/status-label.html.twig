{% if item.free %}
	  <span class="label label-success mrm">{{''|trans}}</span>
{% elseif course.tryLookable and media.storage|default('') == 'cloud' %}
	  <span class="label label-warning mrm">{{'试看'|trans}}</span>
{% endif %}

